# Art credits — Hikmah Noor

All in-page artwork is **original inline SVG** created for this project
(`SiteArt`, `MeaningArt`, `WaqiahArt`, `KalimaHub` components) — no stock,
no owners to attribute, commercial-safe.

Exception: `public/og-cover.jpg` (social share image) predates the SVG
system. Replace it with original art before any commercial use that needs
clean provenance.
